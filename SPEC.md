# YouTubeショート動画 自走化システム仕様書

最終更新: 2026-07-30

## 1. 概要

- **チャンネル**: ほっと一息チャンネル(YouTube、Googleアカウント: `fukugyoconsul@gmail.com`)
- **ジャンル**: エンタメ・雑学系ショート動画
- **目的**: 顔出し・声出しなしで、企画〜投稿〜エンゲージメント対応までを完全自動化する
- **実行環境**: GitHub Actions(クラウド)。ローカルPCの起動状態に依存せず、追加料金なしで動作する

## 2. リポジトリ・認証情報

- **GitHubリポジトリ**: `businessfxtrader-rgb/shorts-entertainment`(非公開)
- **秘密情報の保管場所**: GitHub Secrets(暗号化)
  - `GOOGLE_TTS_API_KEY`: Google Cloud Text-to-Speech用
  - `PEXELS_API_KEY`: Pexels動画検索用
  - `CLAUDE_CODE_OAUTH_TOKEN`: 台本・ジャンル生成用(Claude Pro、長期間有効トークン)
  - `GOOGLE_CLIENT_SECRET_JSON`: YouTube OAuthクライアント情報
  - `YOUTUBE_TOKEN_JSON`: YouTube認証トークン(スコープ: `youtube.force-ssl` + `yt-analytics.readonly`。アップロード・サムネイル・コメント・統計取得・視聴維持率取得すべてに対応。再認証は`scripts/youtube-auth.mjs`をローカルで実行しブラウザ許可)
  - `GOOGLE_SERVICE_ACCOUNT_JSON`: スプレッドシート書き込み用サービスアカウント

## 3. 自動実行スケジュール・バッファ方式

GitHub Actionsの`schedule`(cron)は、公式ドキュメントにも明記されている通り高負荷時に遅延・スキップされることがある(実際にこのリポジトリでも、設定は正しいのに1時間以上発火しない事象を確認済み)。そのため「決まった時刻ちょうどに実行される」ことに依存せず、**予約投稿のバッファ(在庫)を切らさないこと**を目的にした設計にしている。

| ワークフロー | 頻度 | 内容 |
|---|---|---|
| `daily-pipeline.yml` | 毎日 12:13・18:13・0:13・6:13(JST、6時間おき) | 「常に3日分(6本)の予約投稿」を下回っていれば、不足分だけ動画を作成してYouTubeの予約公開に登録する(バッファ方式、詳細は3-1) |
| `weekly-research.yml` | 毎週月曜 9:00(JST) | 過去動画の再生数集計 + AIによる新ジャンルの自動リサーチ・追加 |
| `watchdog.yml` | 毎日 13:15・19:15(JST、内部cron・保険用) | バッファ残量チェック+予約公開の成否確認・シートのステータス確定(詳細は3-2)。メインは外部cron-job.org側の13:03・19:03(3-3参照) |

### 3-1. バッファ方式(予約投稿のストック)
- `scripts/pipeline.mjs`が実行されるたびに、`content/used-topics.json`の`publishedAt`が未来の日時になっている件数(=まだ公開されていない予約済み動画の本数)を数える
- 目標本数(3日分×1日2本=6本)に足りなければ、足りない分だけ「台本生成→TTS→背景取得→BGM選定→字幕書き出し→レンダリング→YouTubeアップロード→シート記入」を実行する。1回の実行での上限は3本(`MAX_NEW_PER_RUN`)
- YouTubeアップロード時は`privacyStatus: private` + `publishAt: <割り当てた投稿枠の日時>`で「予約公開」として登録する。指定した日時になれば、人の操作なしにYouTube側が自動で全体公開に切り替える
- 投稿枠は「今から先の直近のJST 12:00・18:00」を古い順に割り当てる。実行タイミングが多少ズレても、実際に公開される時刻はYouTube側の予約公開機能が正確に守るため影響しない
- ある枠の動画作成が(10回リトライしても)失敗した場合、その枠だけ管理シートに「エラー」を記録して次の枠に進む。バッファに余裕があるため全体を止めない
- サムネイル設定・コメント自動返信は、予約(未公開)状態の動画にも安全に実行できるため変更なし

### 3-2. 自動実行の信頼性対策(watchdog)
`watchdog.yml`は毎日13:03・19:03 JST(投稿時刻12:00・18:00の約1時間後、外部cron-job.org経由がメイン。内部cronは13:15・19:15 JSTの保険用、3-3参照)に、2つの観点で「結果」そのものを直接チェックする(`gh run list`でスケジュール実行の有無を確認する方式(過去の実装)は使わない。「cronが発火したか」よりも「実際に機能しているか」の方が本質的な指標のため)。

- **バッファ残量チェック**(`scripts/check-buffer.mjs`): 予約済み本数が警告ライン(3本)を下回っていれば、`scripts/report-status.mjs`経由で管理シートに「エラー」行を自動追加する
- **公開状況チェック**(`scripts/check-published.mjs`): 予約公開の予定時刻(30分の猶予込み)を過ぎた動画のうち、シート上でまだ「予約済み」のままの行だけを対象に(無駄なAPI消費を避けるため)、YouTube側の実際の`privacyStatus`を確認する。`public`になっていれば「予約済み」→「投稿完了」に更新。過ぎても非公開のままなら「予約済み」→「エラー」に更新しK列に理由を記載する。アップロード自体は成功しているため、`report-status.mjs`による新規行追加ではなく既存行の更新になる点が3-1の失敗時処理と異なる
- **運用上の注意**: cron設定を変更した直後は、GitHub Actions側に新しい設定が反映されるまでにタイムラグがある(体感で1時間以上かかることもある)ため、変更直後の1〜2回分の実行結果だけでは「発火した/しなかった」の判断材料にしない

### 3-3. 外部トリガーによる二重化(cron-job.org)
バッファ方式・watchdogを導入した後も、GitHub Actionsの`schedule`が長時間(週次リサーチで12時間以上、daily-pipeline/watchdogでは数時間)発火しない事象が実測で確認されたため、GitHub外部の無料サービスから確実にトリガーする仕組みを追加している。

- **サービス**: [cron-job.org](https://cron-job.org/)(無料、ユーザー本人のアカウント)
- **仕組み**: GitHubのREST API(`POST /repos/{owner}/{repo}/actions/workflows/{workflow}/dispatches`)を、cron-job.org側のスケジュールから直接叩く。`workflow_dispatch`イベントとして実行されるため、GitHub自身の`schedule`とは独立して動作する
- **認証**: GitHub Fine-grained Personal Access Token(リポジトリを`shorts-entertainment`のみに限定、権限は`Actions: Read and write`のみ)。**このトークンはリポジトリやGitHub Secretsには保存されていない**。cron-job.org側のジョブ設定(リクエストヘッダー)にのみ保持されており、失効・再発行が必要な場合はユーザー本人がGitHubの[Fine-grained tokens設定画面](https://github.com/settings/tokens?type=beta)から行う
- **設定済みジョブ**(cron-job.orgのダッシュボードで管理):
  | ジョブ名 | 対象ワークフロー | スケジュール(UTC) | JST換算 |
  |---|---|---|---|
  | `shorts-daily-pipeline-trigger` | `daily-pipeline.yml` | `20 3,9,15,21 * * *` | 12:20・18:20・0:20・6:20 |
  | `shorts-watchdog-trigger` | `watchdog.yml` | `3 4,10 * * *` | 13:03・19:03 |
- GitHub自身の`schedule`設定(3章の表)は削除せず併存させている。同時刻付近に二重発火しても、バッファが足りていれば何もせず終了するだけなので無害
- 動作確認済み(2026-07-27、両ジョブとも「TEST RUN」でGitHub側から`204 No Content`を確認)

## 4. コンテンツ生成の仕組み

### ジャンル(2026-08-07時点で18種、`scripts/categories.json`で管理)
雑学(ranking・qa) / 語源(ranking・qa) / 都市伝説(ranking・qa) / 文化(ranking・qa) / 心理学(ranking・qa) / 宇宙(simulation) / 人体(ranking・simulation) / 歴史(simulation・qa) / 怖い話(ranking・qa・創作可) / ショートストーリー(simulation・reversal・創作可) / 誕生秘話(qa・ranking) / ライフハック(ranking・qa) / 動物(ranking・qa) / 建築(qa・ranking) / 深海(qa・simulation・ranking) / グルメ(ranking・qa) / 生態(qa・ranking) / 日本(qa・reversal)

2026-08-07、ほぼ全ジャンルに2つ目以降のフォーマットを追加(ユーザー指示: 「深海」を例に、複数フォーマットでいけるものは臨機応変に対応)。判断基準は「同じ題材を別の構成で語っても不自然にならないか」「既存の他ジャンルと内容が丸かぶりしないか」。宇宙のみ、雑学ジャンルが既に宇宙系ランキング動画を生成済みで重複リスクがあったため単一フォーマットのまま据え置いた。`reversal`フォーマット自体もこの際に汎用化し(旧: 「日本の〇〇」決め打ち → 新: 見下す対象はジャンルのbriefが指定)、ショートストーリーにも適用できるようにした。`ranking`/`qa`双方に展開する際、briefに元々「3つ紹介する」「問いかけから結論に導く」のようなフォーマット限定の文言が含まれていたジャンル(人体・歴史・怖い話・誕生秘話・ライフハック・動物・建築・生態)は、`formatが◯◯の回は〜`という分岐文に書き換えて矛盾を解消した

週次リサーチ(`weekly-research.yml`)により今後も自動で増える想定。

- ジャンル名は2〜4文字程度の短い単語1つに統一(助詞・接続詞を含む長い名称は不可)
- 詳細な生成ルールは各ジャンルの`brief`フィールドに記述
- 各ジャンルは管理シート表示用の`displayGenre`を持つ。台本生成・ジャンルローテーション(重み付き抽選)・重複防止は`name`(具体的なジャンル名)単位で行われ、これらの挙動には一切影響しない。`displayGenre`はシートのD列に何を表示するかだけを決める、純粋に見た目のグループ分け
  - デフォルトは`name`と同じ(そのまま表示)
  - 例外: 語源→「雑学」、宇宙/人体/歴史→「シミュレーション」として表示(単体では意味が伝わりにくいため、見た目上まとめて表示するようユーザーが指定)
  - 新ジャンルが週次リサーチで追加される際は、`displayGenre`はデフォルトで`name`と同じ値になる(まとめて表示したい場合は`categories.json`を手動編集する)
- **ジャンルとフォーマットは独立した軸**(2026-08-06〜)。各ジャンルは`formats`(配列)を持ち、1ジャンルが複数フォーマットを扱える(例: 日本ジャンルは`["qa", "reversal"]`)。台本生成のたびに、選ばれたジャンルの`formats`からランダムに1つを選ぶ(`scripts/generate-script.mjs`の`chosenFormat`)。理由: 特定の演出(掌返しストーリーなど)だけに限定すると表現の幅が狭まり、単純な紹介でもバズる余地を逃すため

### フォーマット(4種類)
- **ranking**: フック→第3位→第2位→第1位→締め、のベスト3形式。タイトルには可能であれば『史上最高の』のようなジャンル固有の定番枕詞を先頭に置き、シリーズものとして認識されやすくする(2026-08-06、9M再生の競合動画分析より。詳細は4-3章)
- **simulation**: フック→段階1→段階2→クライマックス、の「もしも〜だったら」思考実験形式(badge非表示)
- **qa**: 「○○って知っていますか?」のような問いかけをフックにし、第3位・第2位で結論を伏せたまま焦らし(伏線)、第1位で真相を明かす形式(badge非表示)。誕生秘話ジャンルで使用
- **reversal**(2026-08-06追加、2026-08-07に汎用化): 何かを見下す・疑うセリフの引用から始まり、架空の一般的人物(実在の個人は特定しない)がそれを体験して価値観が覆っていく「掌返しストーリー」形式(badge非表示)。見下す・疑う対象は特定のジャンルに固定されておらず、各ジャンルのbriefが指定する(例:日本ジャンルなら日本の物事、ショートストーリーなら自由な創作対象)。タイトルは50〜70字程度の長め(冒頭セリフ引用→人物像→体験→強い結果の言葉、という一連の物語をタイトルだけで伝える)。日本ジャンル(qaと並行)・ショートストーリー(simulationと並行)で使用。詳細は4-3章

### ナレーションの尺・密度
- 動画の尺は54〜59秒を目標とする
- narration(読み上げ文)の合計文字数が320〜345字程度になるよう台本生成プロンプトで指定(目安: hook 50〜55字、rank3/rank2/rank1は各65〜85字、outroは50〜55字)
- TTSの読み上げ速度は`speakingRate: 1.15`(`scripts/generate-tts.mjs`)

### ジャンル選定ロジック
- **ローテーション制御(2026-08-08追加)**: 直近(全ジャンル数-1)本以内に使われたジャンルは、今回の抽選候補から除外する。これにより「必ず一巡してから次の巡目に入る」ことが数学的に保証される
  - 導入経緯: 従来は重み(下記)だけでジャンルを決めていたが、一度伸びたジャンルの重みが実績なしジャンルの基礎重み(30)の10倍以上になることがあり、18ジャンル中7ジャンルが一度も生成されないという偏りが発生していた(ユーザー指摘により発覚)
  - 実績が無い/一度も使われていないジャンルは当然この除外対象に入らないため、自動的に最優先の候補になる(コールドスタート対策も兼ねる)
- 上記でローテーション制御された候補の中で、過去の平均再生数 × 視聴維持率倍率(0.5〜2.0倍)に応じた重み付き抽選を行う(同じ巡目内でも、伸びている・最後まで見られているジャンルほど選ばれやすい)
- 視聴維持率倍率の算出方法・データ取得元は4-1章を参照

### 4-1. SEO対策・視聴維持率トラッキング(自走ループ)
再生数だけでは「①表示されない(インプレッション不足)」「②クリックされない」「③最後まで見られない」のどこがボトルネックか判別できないため、YouTube Analytics APIから**視聴維持率**を取得し、ジャンル選定と台本生成の両方に継続的にフィードバックしている。

- **データ取得**(`scripts/track-performance.mjs`、`weekly-research.yml`で週次実行):
  - `youtube.videos.list`で各動画の再生数を取得(従来通り)
  - `youtubeAnalytics.reports.query`で各動画の`averageViewDuration`・`averageViewPercentage`を取得。**このAPIは`dimensions=video`単体では"not supported"エラーになる**ため、必ず`filters=video==id1,id2,...`と組み合わせて呼び出す(検証済みの制約)
  - なお、通常のYouTube Analytics API(`reports.query`)ではインプレッション数・クリック率(CTR)は取得できない。**別のAPIである YouTube Reporting API(`youtubereporting.googleapis.com`)経由でのみ取得可能**(詳細は4-2章)
  - 結果を`content/category-stats.json`(ジャンル別平均視聴維持率)と`content/retention-summary.json`(チャンネル全体平均)に保存
- **台本生成へのフィードバック**(`scripts/generate-script.mjs`):
  - ジャンル選定の重み = 平均再生数 × min(2.0, max(0.5, ジャンル別視聴維持率 ÷ チャンネル全体平均維持率))
  - チャンネル全体の平均視聴維持率が40%未満の場合、プロンプトに追加の強化指示(フックの具体性・情報密度・尻すぼみ防止)を自動挿入
  - 常時適用のルールとして、フック最初の1文の惹きつけ・前置き厳禁を明文化
  - **SEO方針の補正(2026-08-07)**: 検索キーワード最適化(tags・descriptionHook)は引き続きSEOの中心的な柱として重視する。ただしtitleについてのみ、具体的な数字・結論を言い切る「ネタバレ」は避け、核心は動画を見ないとわからない状態を保つ(例:「〜は3つ！」ではなく「まさかの理由」)。ネタバレはクリック後すぐスキップされる原因になるため。検索キーワードはtitleではなく主にtags・descriptionHook側で担う役割分担にする。参考データとして、Shorts再生の9割以上はおすすめフィード経由(視聴維持率・完了率が評価軸)で検索経由は1割未満という調査結果があり、画面内テキスト(caption/テロップ)はタイトルより強いトピック信号として解析されるとされるため、captionへの具体的な数字・固有名詞の記載も引き続き重視する。この方針変更に伴い、既存のネタバレ型タイトル10本(`scripts/_tmp-fix-spoiler-titles.mjs`で一括修正、使い捨てスクリプトのため実行後削除済み)を、内容はそのままにタイトルのみ好奇心を保つ形へ修正済み

### 4-2. 伸び悩み動画の自動SEO再試行・トレンド調査・インプレッション取得
`weekly-research.yml`に以下3つを追加(2026-08-04)。いずれも人の判断を待たず自走する。

- **伸び悩み動画への自動再SEO**(`scripts/auto-retry-seo.mjs`):
  - 公開から4日以上経過した動画を対象に、1日あたり再生数を算出
  - 対象動画群(4日以上経過分)の**中央値**を基準に、その50%を下回る動画を「伸び悩み」と判定(平均ではなく中央値を使うのは、一部の動画が突出して伸びた場合に平均が引っ張られて閾値が狂うのを防ぐため)
  - 該当動画のうち再生数が低い順に最大3本まで、タイトル・タグ・概要欄冒頭をclaude -pで再生成しYouTube・管理シートに反映。動画の内容(トピック)自体は変更せず、動画の削除・再作成も行わない
  - 動画は削除しない方針のため、**一度きりのリトライで終わらせず**、伸び悩みが続く限り何度でも再挑戦する。`seoRetriedAt`(最終リトライ日時)・`seoRetryCount`(通算リトライ回数)・`viewsAtRetry`(リトライ時点の再生数)を`content/used-topics.json`に記録し、前回のリトライから`RETRY_COOLDOWN_DAYS`(7日)経過するまでは同じ動画を再対象にしない、というクールダウン方式で無限連投を防ぎつつ繰り返し再試行する
  - 再評価時の「1日あたり再生数」は、**リトライ済みの動画は公開時からの累計平均ではなく`viewsAtRetry`からの増分ペース(直近のSEO変更後どれだけ伸びたか)**で計算する。公開時からの累計平均のままだと変更前の低迷データでいつまでも薄まってしまい、変更の効果を判別できないため。なお、YouTube Shortsの配信は主に公開直後(48〜72時間以内)のアルゴリズム判断で決まりやすく、後からのメタデータ変更は主に検索経由の発見に効く傾向がある点は留意すること
  - 管理シートへの反映: J列(SEO対策)は毎回最新の内容に書き換え、K列(備考)には「いつ・何回目の再試行を・なぜ行ったか」を1行追記する(既存の備考があれば改行で追加、上書きはしない)。これによりシートを見るだけで各動画のSEO再試行履歴が追える
- **トレンドパターン・テーマ・検索キーワード調査**(`scripts/research-trends.mjs`): claude -p(Web検索)で直近のYouTube Shorts市場の傾向を週次調査し、`content/trend-insights.json`に保存。3種類を調査する: (1)`patterns` = タイトル・フックの構造的な「型」、(2)`themes` = 「海外目線での日本称賛」「動物の見た目とのギャップ」のような、今伸びやすいテーマ・題材の傾向(ジャンル単位の一般的なカテゴリであり特定動画の著作物ではないため、内容面でも遠慮せず具体的に報告する)、(3)`keywords`(2026-08-07追加) = 概要欄・タグでの検索キーワード選定の傾向(タイトルはネタバレ回避を優先する分、検索性は概要欄・タグ側で補うという前提で調査)。特定動画の台本・セリフそのものは書き写さず、あくまで抽象化した「型」「テーマ」「キーワード選定パターン」として記録する(著作権配慮)。`generate-script.mjs`が台本生成のたびに、`patterns`からランダムに1件、`themes`からは選ばれたジャンルに関連するものを優先して1件、`keywords`からランダムに1件、参考として提示する(強制ではなく「使えそうなら使う」扱い)
- **インプレッション・CTR取得**(`scripts/track-reach.mjs`): YouTube Reporting API(通常のAnalytics APIとは別物)でのみ、一般クリエイター向けにもインプレッション数・CTRを含むバルクレポート(CSV、1〜2日遅れで生成)を取得できることが判明。`reportTypeId: "channel_reach_basic_a1"`のジョブを手動で一度だけ作成済み(ジョブIDはスクリプト内にハードコード)。週次でジョブに新しいレポートがないか確認し、あれば`content/reach-stats.json`に取り込む。列名が未確認のため、想定と異なる形式だった場合は`content/reach-raw-<reportId>.csv`に生データを保存して後日調整する設計にしている

### 新ジャンルの自動開拓
週次で`claude -p`(Web検索込み)がトレンドを調査し、以下の条件を満たす新ジャンルのみ自動追加:
- 著作権のあるキャラクター・作品への言及禁止
- 実在の人物(有名人・YouTuber等)を扱う内容禁止
- 投資・金融商品の売買助言、個別の医療・法律相談に該当する内容禁止
- ナレーション+シンプルな背景動画だけで成立する内容に限定
- 提案時に`fiction`(創作可否)も同時に決定する

### 台本生成の安全策
- 悩み相談・個別相談・アフィリエイト誘導は一切含めない
- ジャンルごとに`fiction`フラグを持つ。`fiction: false`(雑学・科学系など)は事実のみ(誇張・不確かな内容は禁止)、`fiction: true`(エンタメ・創作系)は創作物語を許可(ただし実話であるかのように装わない)
- `fiction`の値に関わらず、実在の人物(有名人・YouTuber等)の実名・私生活や、著作権のあるキャラクター・作品への言及は絶対に禁止
- 誤読防止辞書(`content/misreading-dict.json`)により、読み間違いやすい漢字をナレーションのみひらがな化
- 過去に使ったネタ(`content/used-topics.json`)と重複させない
- JSON出力破損防止のため、本文中に二重引用符を使わせない

### 4-3. 競合動画分析からの反映(2026-08-06)
ユーザー指定の伸びている2動画を分析し、判明した要因を反映した:
- **「史上最高の 国別の戦闘艦 TOP6」(@ランキング忍者7、907万再生)**: うちのチャンネルと同じranking形式。ジャンルごとに「史上最高の」のような統一枕詞をタイトル先頭に固定して繰り返し使っており、シリーズとして視聴者に認識されやすくなっていると推測される→ranking形式のtitleInstructionsに反映(4章フォーマット参照)
- **「日本のオタクが街を荒らしに来たぞ!」...(@JAPAN-qw9pu、AI生成・9.1万登録、公開4日で38万再生)**: 日本の物事を見下す・疑うセリフの引用→架空の人物(外国人記者、パスタが苦手な息子、フランス人女性など。実在個人ではない)がそれを体験→価値観が覆る、という「掌返しストーリー」構造をタイトル自体で語り切る(50〜80字程度の長いタイトル)。チャンネル内の全動画がこの型で統一されている→新フォーマット`reversal`として実装(`scripts/generate-script.mjs`のFORMAT_SPECS)。`research-genres.mjs`の週次新ジャンル開拓でも選択可能にした
  - 当初は日本ジャンルの`format`を`qa`→`reversal`に置き換えたが、ユーザーから「掌返しだけでなく単純な日本称賛でもバズる。ジャンルとフォーマットは切り分けるべき」との指摘を受け、`format`(単一)→`formats`(配列)にスキーマを変更。日本ジャンルは`["qa", "reversal"]`とし、生成のたびにランダムでどちらかが使われるようにした(4章「ジャンルとフォーマットは独立した軸」参照)

## 5. 素材生成

| 要素 | 使用サービス | 詳細 |
|---|---|---|
| ナレーション | Google Cloud TTS(Chirp3-HD、`ja-JP-Chirp3-HD-Orus`) | 無料枠(月100万文字)の範囲内 |
| 背景動画 | Pexels API | 各シーンの内容に応じた英語キーワードで検索・縦向き動画を自動取得 |
| BGM | 自前ライブラリ6曲(`scripts/bgm-library.json`) | すべてKevin MacLeod氏の楽曲、CC BY 3.0。曲ごとに動的にクレジット表記を概要欄へ挿入 |

## 6. 書き出し

- Remotion(1080×1920、30fps)
- 背景動画をフルスクリーン表示、上下に暗転グラデーション
- テロップは極太フォント・フェードイン
- BGMは動画全体にループ再生

## 7. 公開時の処理(YouTube Data API)

- **公開設定**: 予約公開(`privacyStatus: private` + `publishAt`)。人の確認は挟まず、割り当てた投稿枠の日時になれば自動で全体公開に切り替わる(バッファ方式の詳細は3-1)
- **カテゴリ**: エンターテインメント(categoryId: 24)
- **子ども向け設定**: 否(`selfDeclaredMadeForKids: false`)
- **タイトル**: ジャンル・フォーマットに応じてSEOを意識した文言を毎回生成(数字・固有名詞・「もしも〜」等の検索されやすい表現)
- **概要欄**: 内容要約+固定の紹介文(X誘導含む)+BGMクレジット+ハッシュタグ
- **ハッシュタグ**: 固定3つ(#雑学 #豆知識 #shorts)+動画内容に応じた動的2つ
- **タグ**: 動画ごとに具体的なキーワードを8個程度
- **SEO対策メモ**: 今回何をSEO対策したかを1〜2文で生成し、管理シートに記録
- **サムネイル**: 動画の一部フレーム(フックのテロップが表示された箇所)を静止画として書き出し、自動設定

## 8. 記録・分析

- **管理シート**: 「YouTube自動化 管理シート」というスプレッドシート内の**「ほっと一息チャンネル」タブ**(サービスアカウント経由で書き込み)
  - **重要**: このスプレッドシートには他にも「TACグループ」(投資系チャンネル、別リポジトリが管理)・「チャンネル管理」(ユーザーが手動で追加した参照用タブ)など複数のタブが同居している。そのため、書き込み先のタブは**必ずタブ名で直接指定する**(`append-sheet.mjs`・`check-published.mjs`・`report-status.mjs`・`retry-seo.mjs`すべて`SHEET_NAME = "ほっと一息チャンネル"`を固定値で指定)。かつて「スプレッドシートの1番目のタブ」を動的取得する方式だったが、新しいタブが先頭に追加されたことで誤ったタブに書き込んでしまう事故が発生したため、この方式に変更した(2026-08-04)
  - 列構成: A=No.(自動採番の数式`=ROW()-1`) / B=動画形式 / C=ステータス / D=ジャンル(`scripts/categories.json`の`displayGenre`。表示用のグループ名で、実際のジャンル名や台本生成の挙動とは別物。詳細は4章) / E=投稿日時(バッファ方式では予約公開の日時=YouTubeが実際に公開する予定時刻) / F=タイトル / G=概要欄全文 / H=ハッシュタグ / I=動画URL / J=SEO対策メモ / K=エラー内容・備考
  - **ステータス(C列)**: 動画作成・予約登録が正常完了した時点で自動で「予約済み」(まだ非公開・E列の日時になれば自動で全体公開される)。予定時刻を過ぎて実際に全体公開されたことをwatchdog(`check-published.mjs`)が確認すると、自動で「投稿完了」に更新される。パイプラインが失敗した場合は自動で「エラー」を記録し、K列にどの段階で何が起きたかを記載する。「予約済み」「投稿完了」「エラー」のいずれにも当てはまらない想定外の事象が起きた場合のために「保留」も用意しており、いずれもK列に理由を記載する(`scripts/report-status.mjs`)
  - 書き込み処理はA列に一切触れない
- **再生数トラッキング**(`content/category-stats.json`): 週次でジャンル別の平均再生数を集計し、次回以降のジャンル選定に反映

### 8-1. 失敗時の自動リトライ
- 台本生成・TTS・動画取得・BGM選定・字幕/概要欄書き出し・動画レンダリング・YouTubeアップロード・シート記入の各工程は、失敗しても1〜3分(ランダム)待って最大10回まで自動的に再試行する(`scripts/pipeline.mjs`の`runWithRetry`)
- 10回すべて失敗した場合のみ、管理シートに「エラー」として記録しパイプラインを終了する
- サムネイル設定・コメント自動返信は失敗しても後続処理をブロックしない「ソフト」実行(`runNodeSoft`、リトライなし)

## 9. エンゲージメント対応

- **コメント自動返信**(`scripts/reply-comments.mjs`)
  - 1回の実行で最大3件まで返信
  - 返信文は15パターン(X誘導あり10パターン/なし4パターン)からランダム選択、確率はX誘導あり70%・なし30%
  - X誘導文言には`https://x.com/kyukei_sokuho`を記載
  - 返信済みコメントは`content/replied-comments.json`で管理し、二重返信を防止
  - 同一文面の連投・大量返信を避けることで凍結リスクを低減

## 10. 既知の制限・保留事項

- **YouTube Data APIの無料枠上限**: 1日あたり約6本のアップロードが上限(現行1プロジェクトの場合)。現在は1日2本(定常時)で運用しており十分な余裕がある
- **Instagram同時投稿**: 未対応(対象外として保留中)
- **投資系チャンネルの新規追加**: 別チャンネル「TACグループ」として、別リポジトリ(`businessfxtrader-rgb/shorts-tac-group`)・別チャットセッションで並行して構築中。このSPEC.mdのアーキテクチャを踏襲しているが、本チャンネルとは完全に独立したリポジトリ・GitHub Secrets・管理シートタブ(同一スプレッドシート内の別タブ「TACグループ」)で運用
- **サムネイル**: YouTube Shorts本編のフィード上には表示されないが、検索結果・チャンネルページ・関連動画には反映される
